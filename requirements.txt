# Base installation (dashboard + data collection + Korean market)
# Run: pip install -e .
#
# With ML models:
#   pip install -e ".[ml-boost]"       # XGBoost/LightGBM/CatBoost
#   pip install -e ".[ml-torch]"       # Transformer + HuggingFace NLP
#   pip install -e ".[ml-tf]"          # TensorFlow/Keras LSTM
#   pip install -e ".[ml-all]"         # All ML
#
# With dev tools:
#   pip install -e ".[dev]"
#
# Full install (everything):
#   pip install -e ".[all]"
#
# Generate pinned lockfile (requires pip-tools):
#   pip-compile pyproject.toml --output-file requirements-lock.txt
#   pip-compile pyproject.toml --extra ml-all --output-file requirements-ml-lock.txt

# This file intentionally delegates to pyproject.toml.
# All version constraints are defined there.
-e .
